# Frontend - Urban Planning Assistant

React frontend with Vite for the chat interface.

## Setup

1. Install dependencies:
```bash
npm install
```

2. Start development server:
```bash
npm run dev
```

The app will be available at `http://localhost:5173`

## Configuration

Update the `API_URL` in `src/App.jsx` if your backend runs on a different port.

